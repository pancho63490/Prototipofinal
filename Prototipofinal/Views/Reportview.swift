import SwiftUI

struct ReportView: View {
    var body: some View {
        VStack {
            Text("Report View")
                .font(.largeTitle)
                .padding()

            Spacer()
        }
        .navigationBarTitle("Report View", displayMode: .inline)
    }
}
